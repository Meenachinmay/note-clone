database:
  host: dev.db.example.com
  port: 5432
  user: user
  password: password
  dbname: notedb
logging:
  level: DEBUG
  format: "[%(asctime)s] [%(levelname)s] [%(name)s] %(message)s"
server:
  port: 8000

jwt:
  secret: "dev_secret_change_me"
  algorithm: "HS256"
  issuer: "note-clone"
  audience: "note-clone-users"
  access_token_ttl_minutes: 60
